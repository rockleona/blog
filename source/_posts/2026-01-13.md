---
title: Rust 學習雜記（一）
date: 2026-01-13 17:49:47
tags:
- dev
- rust
---

## 向「優雅」告白

我很喜歡 Rust 的 `match` 語法，搭配上 `enum` 的使用，你不得遺漏任何一個選項跟可能性。在錯誤處理時也是如此，你要處理 `Ok()` 還有 `Err()`，最好的情況之下，希望你不要 `panic!()`。

我只能說，這裡處處充滿著工程師的浪漫。

## Crates 的設計很有趣

編譯期間要知道你的執行檔跟函式庫的檔案分別是哪個，且模組的引入、使用都需要明確的標示，但又非常的簡明易懂。
